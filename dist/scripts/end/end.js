/* 支付成功页业务逻辑页面 */
require( [ "../scripts/end/config.js" ], function(){
	require( [ "jquery", "cookie", "cart", "href", "jump" ], function( $, cookie, cart, href, jump ){
		// 通信测试 => ok
		
		// 判断登录
		cart;

		// 跳转
		href;

		// 位置调整
		jump;
	} );
} );