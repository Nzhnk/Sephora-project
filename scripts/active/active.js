/* 活动页业务逻辑页面 */
require( [ "../scripts/active/config.js" ], function(){
	require( [ "jquery", "cookie", "cart", "href", "position" ], function( $, cookie, cart, href, position ){
		// 通信测试 => ok
		
		// 判断登录
		cart;

		// 跳转
		href;

		// 位置调整
		position;
	} );
} );